# Stage 11C — Location-equilibrium and positioning repair

**Date:** 2026-09-13  
**Input freeze:** `2a6f5b2519acfbbe1d4e7afcf536baad839830c0`  
**Repair branch:** `repair/stage11c-location-positioning`

## Verdict

**PASS / GO for the Stage 11C repair gate.**

The Stage 11B residual proof and positioning issues have been closed without changing the canonical witness, primitives, or core Protection–Attraction Conflict mechanism.

## 1. Exact location-equilibrium repair

The canonical exact certificate now proves, on the complete policy rectangle `[0,2/25]^2`:

- `0 < u_0=(H+d_B)/(2H) < 1`;
- `0 < u_1=(H+d_A)/(2H) < 1`;
- `-1 < B=(d_A-d_B)/(2H) < 1`;
- the affine Bayesian location best response therefore never clips at 0 or 1;
- the best-response map is a contraction and the simultaneous location-probability fixed point is unique and interior after every admissible first-stage policy deviation.

The information structure is now explicit: each firm privately observes its own location-fit draw after public policies and before simultaneous location choice, and forms beliefs over the rival's private draw.

## 2. Welfare convention robustness

If the selected location-fit payoff is counted in welfare, its aggregate expected value is

`R(D)=H/2-D^2/(2H) <= H/2`.

At every symmetric policy profile, `D=0`, so the added term is maximized at both canonical symmetric candidates. Because the baseline planner objective is already globally maximized at `(0,0)` and the local government's baseline objective is already globally maximized at `alpha` against `alpha`, adding this fit term does not overturn either canonical policy choice.

## 3. General marginal decomposition

Stage 11C adds a witness-independent marginal identity at the symmetric origin. Let

`lambda_0 = partial p / partial a_A |_(0,0)`.

Then

`M_L = M_S/4 + 2 b lambda_0`.

Hence, when `lambda_0>0` and the national marginal value satisfies `M_S<0`, the local marginal incentive remains positive iff

`b > -M_S/(8 lambda_0)`.

If `M_S=E_0+R_0`, with direct engineering effect `E_0>0` and induced private-redundancy response `R_0<0`, a marginal Protection–Attraction Conflict obtains when

`-R_0 > E_0`

and

`8 b lambda_0 > -E_0-R_0`.

This separates the three economically measurable margins: direct protection benefit, induced private redundancy response, and plant-attraction response.

## 4. Literature and claim boundary

The paper now explicitly absorbs two closer precedents:

- Grames et al. (2019): public/existing flood defense can jointly affect firm location and private flood-defense investment;
- Meng, Ding & Wang (2024): local-government competition can impair industrial resilience through different channels.

The novelty claim is narrowed to the full-game policy-ranking reversal generated by protection-induced geographic-redundancy adjustment plus decentralized competition for mobile oligopolistic production. The paper does not claim novelty for policy-induced location, adaptation crowd-out, or local-government competition separately.

## 5. Verification gate

GitHub Actions run `34742437277` completed successfully at branch head `d72f759e3f5c89cf6d8eb376616f1f24db719c3a` before this report-only commit. The full gate passed:

- symbolic threshold certificate;
- exact policy-root certificate;
- Stage 11C exact whole-domain certificate;
- exact location no-clipping and uniqueness certificate;
- numerical/global-deviation stress test;
- nested benchmark recovery;
- pytest 4/4;
- deterministic object generation;
- three-pass LaTeX build.

A final CI run after this report-only commit is required before merge.

## 6. Journal-target assessment

RAND remains an ambitious target, but the manuscript is materially stronger than at Stage 11B. The paper is no longer economically interpretable only through a rational witness: the exact witness supplies the global-equilibrium existence certificate, while the new marginal proposition supplies a transparent general condition for the decentralization wedge.

The remaining risk for RAND is importance/general reach rather than mathematical validity. The next stage should therefore be an editorial/submission-target gate, not another theory reopening unless a new hostile audit finds a substantive defect.
